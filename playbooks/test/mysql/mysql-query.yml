---

- name: Test MySQL query
  hosts: "{{ groups['mysql'][0] }}"
  remote_user: "{{ remote_user }}"
  become: no
  vars:
    mysql_home: "{{ bigdata_home }}/mysql"
  tasks:
    - name: Check Database
      command: "{{ mysql_home }}/bin/mysql -uroot -p{{ mysql.root_pw }} --socket={{ mysql_home }}/mysql.sock -e 'show databases;'"
      register: database_list
      failed_when: false
    - name: Create Database
      command: "{{ mysql_home }}/bin/mysql -uroot -p{{ mysql.root_pw }} --socket={{ mysql_home }}/mysql.sock -e 'create database {{ remote_user }};'"
      when: remote_user not in database_list.stdout
    - name: Check Table
      command: "{{ mysql_home }}/bin/mysql -uroot -p{{ mysql.root_pw }} --socket={{ mysql_home }}/mysql.sock -e 'use {{ remote_user }}; show tables;'"
      register: table_list
      failed_when: false
    - name: Create Table
      command: "{{ mysql_home }}/bin/mysql -uroot -p{{ mysql.root_pw }} --socket={{ mysql_home }}/mysql.sock -e 'create table {{ remote_user }}.query_test (name varchar(50), age int, primary key(name));'"
      when: '"query_test" not in table_list.stdout'
    - name: Insert Table
      command: "{{ mysql_home }}/bin/mysql -uroot -p{{ mysql.root_pw }} --socket={{ mysql_home }}/mysql.sock -e \"insert into {{ remote_user }}.query_test values('myeonghwan Lee', 99),('gisung Lee', 38),('ingyeom Kim', 35);\""
      register: insert_result
      failed_when: >
        ("Duplicate entry" not in insert_result.stderr) and
        (insert_result.stderr != "")
    - name: Select Table
      command: "{{ mysql_home }}/bin/mysql -uroot -p{{ mysql.root_pw }} --socket={{ mysql_home }}/mysql.sock -e 'select * from {{ remote_user }}.query_test order by 2;'"
      register: query_result
    - name: MySQL query result
      debug:
        var: query_result